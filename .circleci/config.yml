version: 2
jobs:
  build:
    docker:
      - image: circleci/openjdk:11-jdk

    working_directory: ~/repo

    environment:
      JVM_OPTS: -Xmx2048m
      TERM: dumb

    steps:
      # compiler, test & run
      - run:
          name: compile, test and run
          command: |
                   sbt -mem 2048 ";clean ;coverage ;test ;coverageReport"
#                   bash <(curl -s https://codecov.io/bash) -t db92a4a7-4ab4-49ab-9ba9-164a43a25009